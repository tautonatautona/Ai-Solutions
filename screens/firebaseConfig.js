import * as FirebaseConfig from '../firebaseConfig';
export const auth = FirebaseConfig.auth;
export const app = FirebaseConfig.app;
export const db = FirebaseConfig.db;
export const rtdb = FirebaseConfig.rtdb;
export const getUserRole = FirebaseConfig.getUserRole;
